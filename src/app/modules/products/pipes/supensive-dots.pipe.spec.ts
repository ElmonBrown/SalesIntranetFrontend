import { SupensiveDotsPipe } from './supensive-dots.pipe';

describe('SupensiveDotsPipe', () => {
  it('create an instance', () => {
    const pipe = new SupensiveDotsPipe();
    expect(pipe).toBeTruthy();
  });
});
